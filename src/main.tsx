import React from "react";
import ReactDOM from "react-dom/client";
import BrickStoveLayoutStudio from "./BrickStoveLayoutStudio";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BrickStoveLayoutStudio />
  </React.StrictMode>
);
